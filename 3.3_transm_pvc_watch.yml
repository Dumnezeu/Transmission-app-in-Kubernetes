# Transmission PVC: watch-pvc

apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: watch-pvc

spec:
  accessModes:
    - ReadWriteMany
  storageClassName: transm-storage-class
  resources:
    requests:
      storage: 5Gi